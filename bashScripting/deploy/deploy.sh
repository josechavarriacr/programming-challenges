#!/bin/bash
# sh deploy.sh --migracion --dev --clean|--quick
echo "You start with $# positional parameters"

# Loop until all parameters are used up
while [ "$1" != "" ]; do
    if [ "$1" == "cheese" ]; then
      
    fi
    shift

done